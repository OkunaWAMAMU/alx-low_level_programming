#include <stdio.h>
/**
 * main - entry point of code
 * Return: return value is 0
 **/

int main(void)
{
	int index = 0;

	while (index < 10)
	{
		printf("%d", index);
		index++;
	}
	printf("\n");
	return (0);
}
