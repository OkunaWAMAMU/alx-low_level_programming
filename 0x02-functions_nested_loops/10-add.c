#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the sum of both integers
 **/

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}

